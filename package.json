{
  "name": "xultra-app",
  "version": "1.0.0",
  "description": "XULTRA — app Android / Desktop",
  "private": true,
  "scripts": {
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "add:android": "npx cap add android",
    "copy-web": "cp -r ../xultra-download/* www/ 2>/dev/null || true",
    "build:android": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0",
    "@capacitor/keyboard": "^6.0.3",
    "@capacitor/splash-screen": "^6.0.3",
    "@capacitor/status-bar": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
