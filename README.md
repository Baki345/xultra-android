# XULTRA Android

App Capacitor qui charge https://xultra.space

## Build auto (GitHub Actions)
Actions → Build XULTRA Android APK → Run workflow  
Artifact: **XULTRA-latest.apk** (icône + splash XULTRA)

## Contenu
- `www/` — shell web
- `resources/` — icon 1024 + splash
- `android-icons/` — mipmap densités
- `scripts/apply-branding.sh` — applique icônes au projet Android
